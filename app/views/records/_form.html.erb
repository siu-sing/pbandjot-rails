<%= form_with(model: record, local: true) do |form| %>
  <% if record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(record.errors.count, "error") %> prohibited this record from being saved:</h2>

      <ul>
        <% record.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :pb_weight %>
    <%= form.text_field :pb_weight %>
  </div>

  <div class="field">
    <%= form.label :pb_time %>
    <%= form.text_field :pb_time %>
  </div>

  <div class="field">
    <%= form.label :pb_date %>
    <%= form.date_select :pb_date %>
  </div>

  <div class="field">
    <%= form.label :notes %>
    <%= form.text_area :notes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
